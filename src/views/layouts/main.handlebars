<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PetCareUS</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Abhaya+Libre:wght@400;500;600;700;800&display=swap" rel="stylesheet">

    {{{_sections.css}}}

    <style>
        :root { --primary-blue: #2d5bff; }
        body { font-family: 'Abhaya Libre', serif; background-color: #f8f9fa; min-height: 100vh; display: flex; flex-direction: column; }
        
        /* Brand Identity */
        .brand-text { font-family: 'Abhaya Libre', serif; font-size: 1.8rem; color: #4a90e2; }
        .brand-icon { transform: rotate(-20deg); }
        
        /* Sidebar & Active States */
        .sidebar .list-group-item.active { background-color: var(--primary-blue); border-color: var(--primary-blue); color: white !important; }
        .nav-link.active { color: var(--primary-blue) !important; font-weight: 700; border-bottom: 2px solid var(--primary-blue); }
        
        /* Helpers */
        .search-input-rounded { border-radius: 50px; }
        .cursor-pointer { cursor: pointer; }

        /* --- MỚI: CSS CHO MÀN HÌNH LOGIN --- */
        /* --- LOGIN SCREEN CSS CẢI TIẾN (GIẢI PHÁP 1) --- */
        .login-background {
            background-image: url('/static/images/bg.png'); 
            /* 1. cover: Luôn phủ kín container, giữ tỷ lệ ảnh, chấp nhận cắt bớt cạnh */
            background-size: cover;
            /* 2. no-repeat: Không lặp lại ảnh */
            background-repeat: no-repeat;
            /* 3. Mặc định trên PC: Căn giữa trung tâm */
            background-position: center center;
            
            position: relative;
            /* Đảm bảo vùng chứa này luôn chiếm toàn bộ chiều cao còn lại */
            flex-grow: 1; 
             /* Thêm min-height để chắc chắn trên màn hình ngắn nó vẫn đủ cao */
            min-height: 500px; 
        }

        /* MEDIA QUERY: Chỉ áp dụng cho màn hình nhỏ (điện thoại, tablet dọc) */
        @media (max-width: 768px) {
            .login-background {
                /* Trên điện thoại, ưu tiên hiển thị phần dưới đáy ảnh (nơi có nhân vật) */
                background-position: center bottom;
            }
        }

        /* Lớp phủ mờ (giữ nguyên) */
        .login-background::before {
            content: '';
            position: absolute;
            top: 0; left: 0; right: 0; bottom: 0;
            background: rgba(255, 255, 255, 0.1); /* Giảm độ tối đi một chút vì ảnh nền sáng */
            z-index: 0;
        }
        /* Card login (giữ nguyên) */
        .login-card {
            position: relative;
            z-index: 1;
            background-color: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.5);
        }
    </style>
</head>

<body>

    {{!-- ================= 1. HEADER DÙNG CHUNG ================= --}}
    {{#if isAuth}}
        {{#if (eq authUser.role 'owner')}}
        {{else}}
            <nav class="navbar navbar-expand-lg bg-white border-bottom sticky-top py-3 px-4 shadow-sm">
                <div class="container-fluid">
                    <a class="navbar-brand d-flex align-items-center fw-bold brand-text" href="/">
                        <i class="bi bi-paw-fill me-2 brand-icon text-primary"></i> PetCareUS
                    </a>
            
                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav">
                        <span class="navbar-toggler-icon"></span>
                    </button>
            
                    <div class="collapse navbar-collapse" id="mainNav">
                        <ul class="navbar-nav mx-auto mb-2 mb-lg-0">
                            
                        </ul>
            
                        <div class="d-flex align-items-center">
                            {{!-- User Profile Dropdown --}}
                            <div class="dropdown">
                                <a href="#"
                                    class="d-flex align-items-center text-decoration-none text-dark dropdown-toggle hidden-arrow"
                                    data-bs-toggle="dropdown">
                                    <div class="text-end me-2 lh-1 d-none d-md-block">
                                        <div class="fw-bold small text-uppercase">{{authUser.role}}</div>
                                        <div class="text-secondary" style="font-size: 0.8rem;">{{authUser.full_name}}</div>
                                    </div>
                                    <i class="bi bi-person-circle fs-2 text-primary"></i>
                                </a>
            
                                <ul class="dropdown-menu dropdown-menu-end shadow border-0 mt-2 rounded-3">
                                    <li><a class="dropdown-item py-2" href="/profile"><i class="bi bi-person me-2"></i>My
                                            Profile</a></li>
                                    
                                    <li><a class="dropdown-item py-2" href="/"><i class="bi bi-key me-2"></i>Change password</a></li>
                                    <li></li>
                                        <hr class="dropdown-divider">
                                    </li>
                                    <li>
                                        <form id="frmSignOut" action="/signout" method="post" style="display: none;"></form>
                                        <a class="dropdown-item py-2 text-danger fw-bold"
                                            href="javascript: document.getElementById('frmSignOut').submit();">
                                            <i class="bi bi-box-arrow-right me-2"></i>Logout
                                        </a>
                                    </li>
                                </ul>
                            </div>
                        </div>
                    </div>
                </div>
            </nav>
        {{/if}}
    {{/if}}


    {{!-- ================= 2. PHẦN THÂN TRANG ================= --}}
    
    {{#if isAuth}}
        {{!-- A. GIAO DIỆN ĐÃ ĐĂNG NHẬP (Giữ nguyên) --}}
        {{#if (eq authUser.role 'owner')}}
                {{{body}}}
            </div>
        {{else}}
            <div class="container-fluid flex-grow-1">
                <div class="row h-100">
                    <nav id="sidebarMenu" class="col-md-2 col-lg-2 d-md-block bg-white sidebar collapse border-end p-3 h-100">
                        <div class="list-group list-group-flush mt-2">
                            {{#if (eq authUser.role 'admin')}}
                                <a href="/admin/customers" class="list-group-item list-group-item-action border-0 rounded-3 mb-1 py-3 fw-bold text-secondary"><i class="bi bi-person-fill me-3"></i> USER</a>
                                <a href="/admin/employees" class="list-group-item list-group-item-action border-0 rounded-3 mb-1 py-3 fw-bold text-secondary"><i class="bi bi-people-fill me-3"></i> EMPLOYEE</a>
                                <a href="/admin/medicines" class="list-group-item list-group-item-action border-0 rounded-3 mb-1 py-3 fw-bold text-secondary"><i class="bi bi-capsule me-3"></i> MEDICINE</a>
                                <a href="/admin/appointments" class="list-group-item list-group-item-action border-0 rounded-3 mb-1 py-3 fw-bold text-secondary"><i class="bi bi-calendar3 me-3"></i> APPOINTMENT</a>
                                <a href="/admin/services" class="list-group-item list-group-item-action border-0 rounded-3 mb-1 py-3 fw-bold text-secondary"><i class="bi bi-tools me-3"></i> SERVICE</a>
                                <a href="/admin/statistical" class="list-group-item list-group-item-action border-0 rounded-3 mb-1 py-3 fw-bold text-secondary"><i class="bi bi-graph-up me-3"></i> STATISTICAL</a>
                            {{/if}}
                            {{#if (eq authUser.role 'veterinarian')}}
                                <a href="/vet/schedule" class="list-group-item list-group-item-action border-0 rounded-3 mb-1 py-3 fw-bold text-secondary"><i class="bi bi-calendar-check me-3"></i> SCHEDULE</a>
                                <a href="/vet/patients" class="list-group-item list-group-item-action border-0 rounded-3 mb-1 py-3 fw-bold text-secondary"><i class="bi bi-heart-pulse me-3"></i> PATIENTS</a>
                                <a href="/vet/medical-records" class="list-group-item list-group-item-action border-0 rounded-3 mb-1 py-3 fw-bold text-secondary"><i class="bi bi-journal-medical me-3"></i> RECORDS</a>
                            {{/if}}
                        </div>
                    </nav>

                    <main class="col-md-10 col-lg-10 ms-sm-auto px-md-4 py-4 bg-light">
                        <div class="row mb-4">
                            <div class="col-md-8 mx-auto"> 
                                <div class="input-group">
                                    <input type="text" class="form-control border-dark search-input-rounded ps-4 bg-white" placeholder="SEARCH DATA..." aria-label="Search">
                                    <span class="position-absolute end-0 top-50 translate-middle-y me-3 z-1 cursor-pointer text-secondary">
                                        <i class="bi bi-search"></i>
                                    </span>
                                </div>
                            </div>
                        </div>
                        {{{body}}}
                    </main>
                </div>
            </div>
        {{/if}}

    {{else}}
        {{!-- B. GIAO DIỆN CHƯA ĐĂNG NHẬP (CÓ ẢNH NỀN) --}}
        <div class="flex-grow-1 d-flex justify-content-center align-items-center login-background">
            
            <div class="card p-4 rounded-4 shadow-lg login-card" style="max-width: 450px; width: 100%;">
                
                <div class="text-center mb-4">
                    <h2 class="fw-bold text-primary">Welcome Back</h2>
                    <p class="text-secondary small">Please enter your details to sign in.</p>
                </div>

                <form method="POST" action="/signin">
                    <div class="mb-3">
                        <label class="form-label fw-bold small text-secondary">Email</label>
                        <input type="email" class="form-control border-dark-subtle rounded-3 py-2" name="email" placeholder="name@example.com" required>
                    </div>
                    <div class="mb-3">
                        <label class="form-label fw-bold small text-secondary">Password</label>
                        <input type="password" class="form-control border-dark-subtle rounded-3 py-2" name="password" placeholder="••••••••" required>
                    </div>
                    <div class="d-flex justify-content-between align-items-center mb-4">
                        <div class="form-check">
                            <input class="form-check-input" type="checkbox" id="rememberMe">
                            <label class="form-check-label small text-secondary" for="rememberMe">Remember me</label>
                        </div>
                        <a href="#" class="text-primary text-decoration-none small fw-bold">Forgot password?</a>
                    </div>
                    <div class="text-center">
                        <button type="submit" class="btn btn-primary rounded-pill px-5 py-2 fw-bold shadow-sm w-100" style="background-color: var(--primary-blue); border: none;">
                            SIGN IN
                        </button>
                    </div>
                    <div class="mt-4 text-center">
                        <span class="text-secondary small">Don't have an account?</span>
                        <a href="/vwAccounts/signup" class="text-primary text-decoration-none fw-bold small ms-1">Create an account</a>
                    </div>
                </form>

            </div>
        </div>
    {{/if}}

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            // Script cho Sidebar & Navbar giữ nguyên
            const sidebar = document.getElementById('sidebarMenu');
            if(sidebar) {
                const currentPath = window.location.pathname;
                sidebar.querySelectorAll('.list-group-item').forEach(link => {
                    if (currentPath.includes(link.getAttribute('href'))) {
                        link.classList.add('active');
                        link.classList.remove('text-secondary');
                    }
                });
            }
            const mainNav = document.getElementById('mainNav');
            if(mainNav) {
                const currentPath = window.location.pathname;
                mainNav.querySelectorAll('.nav-link').forEach(link => {
                    if (link.getAttribute('href') === currentPath) {
                        link.classList.add('active');
                    }
                });
            }
        });
    </script>
    {{{_sections.js}}}
</body>
</html>